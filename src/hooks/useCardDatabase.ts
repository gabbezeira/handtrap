import { useCardDatabase } from '../contexts/CardContext';

// Simply re-exporting logic from context, as the context hook is already robust.
// This matches the file structure requirement.

export { useCardDatabase };
